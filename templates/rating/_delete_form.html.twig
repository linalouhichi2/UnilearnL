<form method="post" action="{{ path('app_rating_delete', {'idRating': rating.idRating}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rating.idRating) }}">
    <button class="btn">Delete</button>
</form>
