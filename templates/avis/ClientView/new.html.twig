{% extends 'ClientBase.html.twig' %}

{% block body %}
<div class="container">

    <div class="card o-hidden border-0 shadow-lg my-5">
        <div class="card-body p-0">
            <!-- Nested Row within Card Body -->
            <div class="row">
                <div class="col-lg-5 d-none d-lg-block bg-register-image"></div>
                <div class="col-lg-7">
                    <div class="p-5">
                        <div class="text-center">
                        </div>
                        {{ include('avis/ClientView/_form.html.twig') }}
                       <a href="{{ path('app_avis_index', {'user_id': user_id, 'idFormation': idFormation}) }}" class="btn btn-primary btn-user btn-block">
    Back to list
</a>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
